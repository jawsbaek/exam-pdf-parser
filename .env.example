# ============================================================
# Exam PDF Parser — Environment Variables
# Copy this file to .env and fill in your values.
# .env 파일로 복사한 후 값을 입력하세요.
# ============================================================

# --- Required ---

# Google Gemini API key (필수: Gemini LLM 백엔드용)
GOOGLE_API_KEY=your-google-api-key-here

# --- Authentication (인증) ---

# Comma-separated list of valid API keys.
# If not set, authentication is DISABLED (open access for development).
# 쉼표로 구분된 API 키 목록. 미설정 시 인증 비활성화 (개발 모드).
# API_KEYS=key1,key2,key3

# --- Rate Limiting (속도 제한) ---

# Max requests per minute per API key/IP (default: 60)
# API 키/IP별 분당 최대 요청 수 (기본값: 60)
# RATE_LIMIT_PER_MINUTE=60

# Max simultaneous parse jobs (default: 10)
# 최대 동시 파싱 작업 수 (기본값: 10)
# MAX_CONCURRENT_PARSES=10

# --- CORS (Cross-Origin Resource Sharing) ---

# Comma-separated list of allowed origins.
# If not set, no CORS origins are allowed (API-only mode).
# 쉼표로 구분된 허용 오리진 목록. 미설정 시 CORS 비활성화.
# CORS_ORIGINS=https://your-frontend.com,https://admin.your-app.com

# --- Server ---

# Uvicorn worker count (default: 2, set to CPU cores for production)
# UVICORN_WORKERS=2
